/** OpenAPI types — regenerate: npm run gen:api (from /docs-json). */
export interface paths {
  "/health": {
    get: {
      responses: {
        200: { description: string };
        503: { description: string };
      };
    };
  };
}
